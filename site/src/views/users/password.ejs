<!DOCTYPE html>
<html lang="en">
<head>
    <%- include('../partials/head')  %>
    <title>Cambiar contraseña</title>
</head>
<body>
    <%- include('../partials/header')  %>

    <main class="password">
        
        <section>
            <h2>Cambiar contraseña</h2>
           
            <form action="/user/password?_method=PUT" method="POST">

                
                <input type="password" name="contraseña" id="password" placeholder="Contraseña actual" >
                <% if (locals.errors && errors.contraseña) { %>
                    <small class="errors"><%= errors.contraseña.msg %> </small>

                <% } %>
               
                <input type="password" name="contraseña2" id="password" placeholder="Nueva contraseña" >
                <% if (locals.errors && errors.contraseña2) { %>
                    <small class="errors"><%= errors.contraseña2.msg %> </small>

                <% } %>

            
            <button type="submit" >Cambiar contraseña</button>
            </form>
        </section> 
    </main>

    <%- include('../partials/footer')  %> 

    
</body>
</html>